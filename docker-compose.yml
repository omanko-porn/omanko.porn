version: '3.0'
services:
  db:
    image: postgres:alpine
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
  redis:
    image: redis:alpine
    volumes:
      - ./data/redis:/data
  web:
    restart: always
    image: gargron/mastodon:latest
    env_file: .env.production
    command: ./bin/rails s -p 3000
    depends_on:
      - db
      - redis
    volumes:
      - ./public/assets:/mastodon/public/assets
  streaming:
    restart: always
    image: gargron/mastodon:latest
    env_file: .env.production
    command: yarn start
    depends_on:
      - db
      - redis
  sidekiq:
    restart: always
    image: gargron/mastodon:latest
    env_file: .env.production
    command: ./bin/bundle exec sidekiq -q default -q mailers -q pull -q push
    depends_on:
      - db
      - redis
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
      - "10080:10080"
    depends_on:
      - web
      - streaming
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./config/nginx:/etc/nginx:ro
      - ./public:/usr/share/nginx/html:ro
